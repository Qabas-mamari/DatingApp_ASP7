
/**
 * Interfaces in TypeScript are used for type-checking and enforcing a specific structure on objects.
 * They help catch errors and provide better tooling support in development environments.
 */
export interface User{
  username: string;
  token : string;
}
